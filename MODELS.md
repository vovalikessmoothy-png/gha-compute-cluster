# Матрица моделей: 2 vCPU / ~7 ГБ RAM / только CPU

Правило отбора: модель + рантайм влезают в ~5–6 ГБ RAM (оставляем запас ОС),
и одна задача считается секунды, а не минуты. GPU нет — берём квантованные GGUF
(llama.cpp) и ONNX int8. Несколько pre-deploy профилей под разные цели.

| Профиль | Модель | Формат/квант | RAM | Скорость на 2 vCPU | Назначение |
|---|---|---|---|---|---|
| **embed** | bge-small-en-v1.5 / multilingual-e5-small | ONNX int8, ~130 МБ | ~0.5 ГБ | ~50–150 док/с | Эмбеддинги для поиска/RAG |
| **rerank** | bge-reranker-base | ONNX int8, ~280 МБ | ~0.8 ГБ | ~30–80 пар/с | Реранк выдачи |
| **classify** | MiniLM-L6 / distilbert-sst2 | ONNX int8, ~90 МБ | ~0.4 ГБ | сотни/с | Классификация, интент, тональность |
| **llm-nano** | Qwen2.5-0.5B-Instruct | GGUF Q4_K_M, ~0.4 ГБ | ~1 ГБ | ~30–60 tok/s | Короткие ответы, форматирование, извлечение |
| **llm-small** | Qwen2.5-1.5B / Llama-3.2-1B | GGUF Q4_K_M, ~1 ГБ | ~2 ГБ | ~12–25 tok/s | Суммаризация, JSON-extract, лёгкий чат |
| **llm-mid** | Phi-3.5-mini-3.8B / Qwen2.5-3B | GGUF Q4_K_M, ~2.3 ГБ | ~4 ГБ | ~5–10 tok/s | Качественнее, но медленно — только короткий вывод |
| **asr** | whisper.cpp base / small | GGUF, 140/460 МБ | ~1–1.5 ГБ | ~2–6× realtime | Транскрипция аудио батчами |
| **ocr** | RapidOCR (ONNX) | ONNX, ~15 МБ | ~0.5 ГБ | десятки стр/мин | OCR документов |
| **bg-remove** | RMBG-1.4 / u2net | ONNX, ~170 МБ | ~1.5 ГБ | ~1–3 изобр/с | Удаление фона |

## Что НЕ брать на CPU-раннер
- SD/Flux и любую генерацию изображений — минуты на кадр, не окупается.
- LLM > 7B даже в Q4 — впритык по RAM и <2 tok/s.
- Реалтайм-стриминг токенов с низкой задержкой — холодный старт мешает.

## Рекомендуемый pre-deploy набор (несколько целей сразу)
1. `embed` — самый ходовой, дёшево, высокий throughput → эмбеддинги-как-сервис.
2. `rerank` — пара к embed для поиска/RAG.
3. `llm-nano` (Qwen2.5-0.5B) — extract/format/classify через генерацию.
4. `llm-small` (Qwen2.5-1.5B) — суммаризация и JSON.
5. `asr` (whisper base) — транскрипция.

Каждый профиль = свой warm-pool (свои репо/jobs), чтобы модель грузилась один раз
и держалась в RAM весь 6-часовой жизненный цикл воркера. Профиль воркера задаётся
входным параметром `profile` при dispatch.
