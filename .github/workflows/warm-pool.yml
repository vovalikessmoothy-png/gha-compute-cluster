# DEPRECATED — заменён на worker.yml (Python-профили + pull-очередь).
# Старая ollama-версия удалена. Используй .github/workflows/worker.yml.
name: warm-pool (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. Use worker.yml (warm-pool with Python profiles)."
